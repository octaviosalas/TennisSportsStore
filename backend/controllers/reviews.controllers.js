import Reviews from "../models/reviews.js";

export const saveReview = (req, res) => { 

        const {userId, name, punctuation, review} = req.body
        console.log(req.body)

   

}

export const getReviews = (req, res) => { 
   
}